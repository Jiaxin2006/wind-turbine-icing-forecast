# 课程大实验 Proposal

## 题目

风机结冰相关工况下的时间序列预测与集成建模研究

## 选题背景意义

风机在低温、大风等环境下可能出现结冰或结冰相关异常工况，这会改变设备运行状态及功率输出规律，增加预测难度。若对所有状态统一建模，模型容易受到数据分布变化影响，导致误差增大、泛化能力下降。因此，针对结冰相关工况开展风机运行功率预测，具有明确的工程价值和方法意义。

本课题以风机运行功率 `OT` 为核心预测对象，其中 `OT` 明确指风机运行功率；“工况”指由温度、风速及历史运行状态共同决定的数据状态；“结冰相关工况”指由低温等环境特征诱发、并可能对应不同输出规律的一类运行状态。该问题同时涉及回归学习、序列学习、无监督学习、集成学习与实验设计原则，和课程内容契合度较高。<mccoremem id="03g314ta3tplpklx4dv4lncdf" />

## 拟采用的特征与算法

拟采用的特征主要包括温度、风速等环境变量，风机运行功率 `OT` 的滞后项，以及少量滚动统计特征；对于序列模型，再将过去若干时间步组成输入窗口进行预测。这样设计的原因是环境变量能够反映结冰相关条件，历史项和窗口特征能够刻画时间依赖。<mccoremem id="03g314ta3tplpklx4dv4lncdf" />

算法上，计划先以 Random Forest、SVR 作为回归基线，再比较 CNN、LSTM、Transformer 等序列模型；在此基础上，进一步尝试 stacking 集成与 KMeans 工况划分，并将 CNN-LSTM-Attention 类模型作为扩展方案。整体方法分别对应课程中的回归学习、序列学习、无监督学习和集成学习。

## 数据与实验方案

本研究拟使用国网冀北电力有限公司承德供电公司提供的风机运行数据。数据时间跨度为 2024 年 2 月全月，共 41,760 条按分钟采样的时序观测，包含时间戳、温度、风速及目标变量 `OT` 等字段。该数据具有典型的高频时间序列特征，适合开展短时预测、时序建模与工况划分研究。

实验中将先对数据按时间排序，并进行缺失值处理、特征构造和标准化；随后按照时间顺序划分训练集、验证集和测试集，避免未来信息泄露。实验流程上，首先比较基础模型与序列模型的预测性能；其次构建 stacking 集成模型，考察不同学习器的互补性；最后结合 KMeans 工况划分，分析分工况建模对结冰相关状态预测的影响。若时间允许，再补充混合模型和统计检验实验。

## 评价方案与预期结果

评价指标主要采用 MAE、RMSE 和 MAPE，并结合预测曲线、残差分布和分工况误差做补充分析。预期结果方面，本课题希望在风机运行功率 `OT` 预测上建立一套兼顾准确性与解释性的建模流程，明确传统模型、深度序列模型和集成模型在该任务上的适用性差异；同时验证 stacking 与工况划分是否能够提高模型稳健性，并为结冰相关状态识别和后续决策支持提供依据。即使复杂模型未必在所有指标上都优于传统方法，该实验仍能够说明不同方法在高频时序数据和异质工况条件下的适用边界。
